import { AuthenticatedUserDTO } from './authenticated-user-dto';

describe('AuthenticatedUserDTO', () => {
  it('should create an instance', () => {
    expect(new AuthenticatedUserDTO()).toBeTruthy();
  });
});
