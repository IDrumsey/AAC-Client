import { LocationPictureDTO } from './location-picture-dto';

describe('LocationPictureDTO', () => {
  it('should create an instance', () => {
    expect(new LocationPictureDTO()).toBeTruthy();
  });
});
