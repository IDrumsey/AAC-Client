import { CenterAnimalsMetaData } from './center-animals-meta-data';

describe('CenterAnimalsMetaData', () => {
  it('should create an instance', () => {
    expect(new CenterAnimalsMetaData()).toBeTruthy();
  });
});
